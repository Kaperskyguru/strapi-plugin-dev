{
  "extends": "@strapi/typescript-utils/tsconfigs/admin",

  "compilerOptions": {
    "target": "ESNext",
    "strict": true
  },

  "include": ["admin", "custom.d.ts", "tailwind.config.js"],

  "exclude": [
    "node_modules/",
    "dist/",

    // Do not include server files in the server compilation
    "server/",
    // Do not include test files
    "**/*.test.ts"
  ]
}
